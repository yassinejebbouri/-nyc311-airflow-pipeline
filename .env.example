cat > .env.example << 'EOF'
# Airflow UID (Mac users usually have 501)
AIRFLOW_UID=501

# Postgres (local dev)
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# pgAdmin (local dev)
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=root
EOF